import {Image} from 'react-native'

const LogoTitle = () => {
  return(
    <Image
      style={{width:30,height:30}}
      source = {require('../assets/public_data.png')}
    />
  )
}

export default LogoTitle
